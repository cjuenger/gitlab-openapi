type: object
properties:
  new_name:
    type: string
    description: Required if `color` is not provided!
  color:
    type: string
    description: Required if `new_name` is not provided!
  description:
    type: string
  priority:
    type: integer
    nullable: true