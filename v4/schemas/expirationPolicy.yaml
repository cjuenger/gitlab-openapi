type: object
properties:
  cadence:
    $ref: "../../enums/cadence.yaml"
  enabled:
    type: boolean
  keep_n:
    description: Keep n tags per image name
    type: integer
  older_than:
    $ref: "../../enums/olderThan.yaml"
  name_regex:
    type: string
  name_regex_keep:
    type: string
    nullable: true
  next_run_at:
    type: string
    format: date-time